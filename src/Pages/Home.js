import React, { Fragment } from "react";
import AllPosts from "../Components/AllPosts";

const Home = () => {
  return (
    <Fragment>
        <AllPosts />
    </Fragment>
  );
};

export default Home;
